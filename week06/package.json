{
  "name": "week06",
  "version": "1.0.0",
  "description": "Написать клас со следующим API:",
  "main": "index.js",
  "scripts": {
    "start": "node_modules/.bin/nodemon --debug index.js --DEBUG=*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cluster": "*",
    "config": "*"
  },
  "devDependencies": {
    "debug": "^2.2.0",
    "nodemon": "^1.9.2"
  }
}
